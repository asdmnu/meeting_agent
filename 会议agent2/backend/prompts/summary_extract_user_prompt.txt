请根据下面的会议文本，提取结构化会议结果，并输出 JSON。

最近会议记忆（仅用于区分历史会议与当前会议，禁止把其中未在当前会议文本出现的信息写入当前结果）：
{recent_meetings_context}

JSON schema:
{{
  "meeting_topic": "string",
  "summary": "string",
  "key_points": ["string"],
  "decisions": ["string"],
  "action_items": [
    {{
      "task": "string",
      "owner": "string",
      "deadline": "string",
      "status": "pending"
    }}
  ],
  "risks": ["string"],
  "open_questions": ["string"]
}}

会议文本如下：
{clean_transcript_text}
